
{% macro hmpoFlashCard(params) %}

    <div class="flash-card{%- if params.classes %} {{ params.classes }}{% endif %}" {%- for attribute, value in params.attributes %} {{ attribute }}="{{ value }}"{% endfor -%}>
        {{- params.content if params.content }}{{- caller() if caller }}
    </div>
{% endmacro %}
