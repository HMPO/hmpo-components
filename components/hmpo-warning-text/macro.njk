
{% macro hmpoWarningText(params) %}
    {%- from "govuk/components/warning-text/macro.njk" import govukWarningText %}
    {{ govukWarningText(merge(params, { html: caller() if caller else params.html })) }}
{% endmacro %}
